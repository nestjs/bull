export enum BullQueueGlobalEvents {
  ERROR = 'error',
  WAITING = 'waiting',
  ACTIVE = 'active',
  STALLED = 'stalled',
  PROGRESS = 'progress',
  COMPLETED = 'completed',
  FAILED = 'failed',
  PAUSED = 'paused',
  RESUMED = 'resumed',
  CLEANED = 'cleaned',
  DRAINED = 'drained',
  REMOVED = 'removed',
}
